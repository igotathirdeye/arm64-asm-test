# ARM64-asm-test
uhhh just click Download as ZIP

if you're on like x86_64/anything not ARM64, go into the qemu-stuff folder and do what the README in there tells you to do

> [!NOTE]
> for ARM64 just run these commands:
> 
> `as test.s -o test.o`
>
> `ld test.o -o test`
>
> to run the test file:
> 
> `./test`
>
> `echo $?`
